# 🏗️ Архитектура SignVoiceAI Modern GUI

## 📐 Общая структура

```
┌─────────────────────────────────────────────────────────────────┐
│                    SignVoiceAI Application                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────┐         ┌──────────────────────────┐   │
│  │   GUI Layer       │────────▶│   Business Logic Layer   │   │
│  │  (main_modern_    │         │   (ModernSignVoiceGUI)   │   │
│  │   gui.py)         │         │                          │   │
│  └───────────────────┘         └──────────────────────────┘   │
│           │                              │                     │
│           │                              │                     │
│           ▼                              ▼                     │
│  ┌───────────────────┐         ┌──────────────────────────┐   │
│  │  CustomTkinter    │         │   Core Components        │   │
│  │  Widgets          │         │   - Camera               │   │
│  │  - CTkFrame       │         │   - GestureDetector      │   │
│  │  - CTkButton      │         │   - GestureModel         │   │
│  │  - CTkSlider      │         │   - TextToSpeech         │   │
│  │  - CTkCanvas      │         │                          │   │
│  └───────────────────┘         └──────────────────────────┘   │
│           │                              │                     │
│           │                              │                     │
│           ▼                              ▼                     │
│  ┌───────────────────┐         ┌──────────────────────────┐   │
│  │   Tkinter Core    │         │   External Libraries     │   │
│  │   (базовый слой)  │         │   - OpenCV               │   │
│  │                   │         │   - Mediapipe            │   │
│  │                   │         │   - PyTorch              │   │
│  │                   │         │   - pyttsx3              │   │
│  └───────────────────┘         └──────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🧩 Компоненты системы

### 1. GUI Layer (Визуальный слой)

```python
class ModernSignVoiceGUI:
    """
    Главный класс GUI приложения
    """
    
    Компоненты:
    ├── Video Panel      # Видеопоток и статус
    ├── Control Panel    # Кнопки управления
    ├── Status Panel     # Информация о жесте
    ├── History Panel    # История распознаваний
    ├── Chart Panel      # График уверенности
    └── Settings Panel   # Настройки приложения
```

### 2. Business Logic Layer (Бизнес-логика)

```python
Core Methods:
├── __init__()              # Инициализация
├── init_components()       # Настройка компонентов
├── create_gui()            # Создание интерфейса
├── process_video()         # Обработка видео (Thread)
├── toggle_recognition()    # Старт/Стоп
├── update_status_display() # Обновление статуса
├── update_chart()          # Обновление графика
├── add_to_history()        # Добавление в историю
└── on_closing()            # Завершение работы
```

### 3. Core Components (Основные компоненты)

```python
Components:
├── Camera              # Захват видео
│   └── utils/camera.py
│
├── GestureDetector     # Обнаружение жестов
│   └── utils/gestures.py
│   └── Mediapipe Hands
│
├── GestureModel        # Классификация
│   └── model/gesture_model.py
│   └── PyTorch Neural Network
│
└── TextToSpeech        # Озвучивание
    └── utils/speech.py
    └── pyttsx3
```

---

## 🔄 Поток данных

```
  Камера
    │
    ▼
[Захват кадра]
    │
    ▼
GestureDetector (Mediapipe)
    │
    ├──► [Рука не обнаружена] ──► Показать "Покажите жест"
    │
    ▼
[21 точка руки]
    │
    ▼
[Нормализация координат]
    │
    ▼
GestureModel (PyTorch)
    │
    ├──► Название жеста
    └──► Уверенность (0.0-1.0)
         │
         ▼
    [Проверка стабильности]
         │
         ├──► Нестабильно ──► Продолжить отслеживание
         │
         ▼
    [Стабильный жест]
         │
         ├──► Добавить в историю
         ├──► Обновить график
         ├──► Озвучить через TTS
         └──► Обновить статус в GUI
              │
              ▼
         [Отобразить на экране]
```

---

## 🧵 Многопоточность

```
Main Thread (GUI)
│
├─► Event Loop (CustomTkinter)
│   ├─► Обработка кнопок
│   ├─► Обновление виджетов
│   ├─► Отрисовка интерфейса
│   └─► Обновление Canvas
│
└─► Video Thread (Daemon)
    ├─► Захват кадров
    ├─► Обнаружение жестов
    ├─► Классификация
    └─► Обновление данных
        └─► (Thread-safe updates в GUI)
```

### Thread Safety

```python
# GUI обновляется из video thread
def update_status_display(gesture, confidence):
    """Thread-safe обновление GUI"""
    # CustomTkinter automatically handles thread safety
    self.gesture_value.configure(text=gesture)
    self.confidence_value.configure(text=f"{confidence:.1%}")
```

---

## 📊 Структура данных

### Состояние приложения

```python
State Management:
├── is_running: bool                      # Активность распознавания
├── current_gesture: str | None           # Текущий жест
├── last_gesture: str | None              # Последний озвученный
├── gesture_confidence: float             # Уверенность [0.0-1.0]
├── gesture_stable_count: int             # Счетчик стабильности
├── gesture_history: deque(maxlen=20)     # История жестов
└── confidence_history: deque(maxlen=50)  # История уверенности
```

### Настройки

```python
Settings (Tkinter Variables):
├── stability_threshold: IntVar (1-10)         # Порог стабильности
├── min_confidence: DoubleVar (0.1-1.0)        # Мин. уверенность
├── speech_rate: IntVar (100-200)              # Скорость речи
└── speech_volume: DoubleVar (0.0-1.0)         # Громкость
```

---

## 🎨 GUI Компоненты

### Иерархия виджетов

```
CTk Root Window
│
└─── Main Container (CTkFrame)
     │
     ├─── Left Panel (CTkFrame)
     │    │
     │    ├─── Video Panel (CTkFrame)
     │    │    ├─── Header (CTkFrame)
     │    │    │    ├─── Title (CTkLabel)
     │    │    │    └─── Status Indicator (CTkLabel)
     │    │    └─── Video Canvas (CTkCanvas)
     │    │
     │    ├─── Control Panel (CTkFrame)
     │    │    └─── Buttons (CTkButton × 3)
     │    │
     │    └─── Status Panel (CTkFrame)
     │         └─── Status Cards (CTkFrame × 3)
     │
     └─── Right Panel (CTkFrame)
          │
          ├─── History Panel (CTkFrame)
          │    └─── Scrollable Frame (CTkScrollableFrame)
          │         └─── Entry Cards (CTkFrame × N)
          │
          ├─── Chart Panel (CTkFrame)
          │    └─── Chart Canvas (CTkCanvas)
          │
          └─── Settings Panel (CTkFrame)
               ├─── Sliders (CTkSlider × 4)
               └─── Info Card (CTkFrame)
```

---

## 🔌 Интеграция с библиотеками

### OpenCV
```python
Использование:
├── cv2.VideoCapture()      # Захват с камеры
├── cv2.cvtColor()          # BGR → RGB
├── cv2.resize()            # Масштабирование
└── cv2.flip()              # Отзеркаливание (опц.)
```

### Mediapipe
```python
Использование:
└── mediapipe.solutions.hands
    ├── Hands()             # Детектор рук
    └── process()           # Обработка кадра
        └── 21 landmark × (x, y, z)
```

### PyTorch
```python
Использование:
└── torch.nn.Module
    ├── GestureClassifier   # Модель
    ├── forward()           # Прямой проход
    └── predict()           # Предсказание
        └── (gesture, confidence)
```

### pyttsx3
```python
Использование:
├── pyttsx3.init()          # Инициализация
├── setProperty('rate')     # Скорость
├── setProperty('volume')   # Громкость
└── say() / runAndWait()    # Озвучивание
```

### CustomTkinter
```python
Использование:
├── CTk()                   # Главное окно
├── CTkFrame()              # Панели
├── CTkButton()             # Кнопки
├── CTkSlider()             # Слайдеры
├── CTkLabel()              # Текст
├── CTkCanvas()             # Отрисовка
└── CTkScrollableFrame()    # Прокрутка
```

---

## ⚡ Жизненный цикл приложения

```
1. Инициализация
   ├── Загрузка модели
   ├── Инициализация Mediapipe
   ├── Инициализация TTS
   └── Инициализация камеры
   
2. Создание GUI
   ├── Настройка стилей
   ├── Создание панелей
   └── Привязка событий
   
3. Main Loop (ожидание)
   └── Обработка событий GUI
   
4. Пользователь нажимает "Старт"
   ├── Открытие камеры
   ├── Запуск video thread
   └── Обновление GUI
   
5. Video Processing Loop
   ├── Захват кадра
   ├── Обнаружение руки
   ├── Классификация жеста
   ├── Проверка стабильности
   ├── Озвучивание (если нужно)
   └── Обновление GUI
   
6. Пользователь нажимает "Стоп"
   ├── Остановка video thread
   ├── Закрытие камеры
   └── Обновление GUI
   
7. Закрытие приложения
   ├── Остановка всех потоков
   ├── Освобождение ресурсов
   └── Завершение
```

---

## 🔒 Управление ресурсами

### Камера
```python
Resource Management:
├── Open: camera.open()
├── Use: camera.read()
└── Close: camera.release()
```

### Mediapipe
```python
Resource Management:
├── Init: GestureDetector()
├── Use: detector.detect(frame)
└── Close: detector.close()
```

### TTS
```python
Resource Management:
├── Init: TextToSpeech()
├── Use: tts.speak(text)
└── Stop: tts.stop()
```

### GUI
```python
Resource Management:
├── Create: CTk widgets
├── Update: configure() methods
└── Destroy: destroy() / on_closing()
```

---

## 📈 Оптимизации

### 1. Память
```python
- deque(maxlen=N) для автоматического ограничения
- Удаление старых виджетов истории
- Переиспользование ImageTk объектов
- Daemon threads для автоочистки
```

### 2. Производительность
```python
- Отдельный поток для видео
- Canvas для быстрой отрисовки
- Минимизация GUI updates
- Efficient image conversion
```

### 3. Отклик
```python
- Thread-safe GUI updates
- Non-blocking video processing
- Асинхронное озвучивание
- Плавная прокрутка (ScrollableFrame)
```

---

## 🎯 Дизайн-паттерны

### 1. MVC (Model-View-Controller)

```
Model:
├── GestureModel (модель ML)
├── Camera (источник данных)
└── State variables (состояние)

View:
├── CustomTkinter widgets
└── GUI components

Controller:
├── ModernSignVoiceGUI class
└── Event handlers
```

### 2. Observer Pattern

```python
# GUI наблюдает за изменениями в video thread
Video Thread → State Change → GUI Update
```

### 3. Singleton Pattern

```python
# Единственный экземпляр для каждого ресурса
- One Camera instance
- One GestureDetector instance
- One GestureModel instance
- One TTS instance
```

---

## 🔗 Зависимости между модулями

```
main_modern_gui.py
├── model/gesture_model.py
│   └── torch
│
├── utils/camera.py
│   └── opencv-python
│
├── utils/gestures.py
│   └── mediapipe
│
└── utils/speech.py
    └── pyttsx3
```

---

## 🧪 Тестирование

### Режимы работы

```python
1. С обученной моделью:
   --model path/to/model.pth
   
2. Без модели (dummy mode):
   use_dummy=True
   → Случайные жесты для демо
   
3. Различные камеры:
   --camera 0, 1, 2, ...
```

---

## 📊 Метрики производительности

```
Video Processing:
├── FPS: ~30 (зависит от камеры)
├── Latency: <100ms (обнаружение)
└── Throughput: Real-time

GUI Updates:
├── Frequency: Per frame
├── Render time: <16ms
└── Response: Immediate

Memory:
├── Base: ~300MB
├── Peak: ~400MB
└── Stable: ~350MB
```

---

## 🔮 Расширяемость

### Добавление новых жестов

```python
1. Собрать данные:
   train_collect_data.py --gesture NewGesture
   
2. Обучить модель:
   train_model.py --data data/
   
3. Использовать:
   main_modern_gui.py --model models/new_model.pth
```

### Добавление новых GUI элементов

```python
def create_new_panel(self, parent):
    """Добавить новую панель"""
    new_panel = ctk.CTkFrame(parent, ...)
    new_panel.pack(...)
    # Добавить виджеты
```

### Добавление новых настроек

```python
# В __init__
self.settings['new_setting'] = ctk.DoubleVar(value=0.5)

# В create_settings_panel
new_slider = ctk.CTkSlider(..., variable=self.settings['new_setting'])
```

---

<div align="center">

## 🏗️ Архитектура SignVoiceAI

**Модульная • Расширяемая • Производительная**

[Вернуться к документации](README_MODERN_GUI.md)

</div>

