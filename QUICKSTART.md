# 🚀 Быстрый старт - SignVoiceAI

Краткое руководство для запуска приложения распознавания жестов.

## ⚡ Установка

1. **Клонируйте проект** (если еще не сделали)
   ```bash
   git clone <your-repo>
   cd prototype3
   ```

2. **Установите зависимости**
   ```bash
   pip install -r signvoice_ai/requirements.txt
   ```
   
   Или с виртуальным окружением:
   ```bash
   python -m venv venv
   venv\Scripts\activate  # Windows
   # source venv/bin/activate  # Linux/Mac
   pip install -r signvoice_ai/requirements.txt
   ```

## 🎨 Запуск с новым GUI (рекомендуется)

### Вариант 1: Из корневой папки
```bash
python main.py --gui
```

### Вариант 2: Напрямую
```bash
python signvoice_ai/main_gui.py
```

### С параметрами
```bash
# С обученной моделью
python main.py --gui --model signvoice_ai/models/gesture_model.pth

# Выбор камеры (если у вас несколько камер)
python main.py --gui --camera 1
```

## 🖥️ Запуск классического интерфейса

```bash
python main.py
```

## 📖 Использование

1. **Запустите приложение** (см. команды выше)

2. **В новом GUI:**
   - Нажмите кнопку **"▶ Старт"**
   - Покажите жест перед камерой
   - Держите жест стабильно несколько секунд
   - Жест будет распознан и озвучен!
   
3. **В классическом интерфейсе:**
   - Покажите жест перед камерой
   - Нажмите **Q** для выхода
   - Нажмите **R** для повтора последнего жеста

## 🎯 Распознаваемые жесты

По умолчанию (в режиме демонстрации):
- **Hello** - Приветствие
- **Thanks** - Спасибо
- **Yes** - Да
- **No** - Нет
- **Love** - Люблю

> **Примечание:** Без обученной модели приложение работает в режиме демонстрации (случайные жесты).

## ⚙️ Основные настройки (новый GUI)

В правой панели настроек вы можете изменить:

- **Порог стабильности (1-10)** - сколько кадров жест должен быть стабильным перед распознаванием
  - Меньше = быстрее, но менее точно
  - Больше = медленнее, но точнее
  
- **Минимальная уверенность (0.1-1.0)** - минимальный порог уверенности
  - Меньше = больше ложных срабатываний
  - Больше = меньше распознаваний, но точнее
  
- **Скорость речи (100-200)** - скорость озвучивания
  
- **Громкость речи (0.0-1.0)** - громкость озвучивания

## 🎓 Обучение своей модели

Если хотите обучить модель на своих жестах:

1. **Соберите данные:**
   ```bash
   python signvoice_ai/train_collect_data.py --gesture Hello --output signvoice_ai/data
   ```
   - Нажимайте **SPACE** для сохранения каждого образца
   - Соберите 50-100+ образцов для каждого жеста
   - Нажмите **Q** для завершения

2. **Обучите модель:**
   ```bash
   python signvoice_ai/train_model.py --data signvoice_ai/data --output signvoice_ai/models/gesture_model.pth
   ```

3. **Запустите с обученной моделью:**
   ```bash
   python main.py --gui --model signvoice_ai/models/gesture_model.pth
   ```

## 🐛 Проблемы?

### Камера не открывается
```bash
# Попробуйте другой индекс камеры
python main.py --gui --camera 1
```

### Ошибка импорта модулей
```bash
# Переустановите зависимости
pip install -r signvoice_ai/requirements.txt --upgrade
```

### PyTorch не устанавливается
```bash
# Установите PyTorch с официального сайта
# https://pytorch.org/get-started/locally/

# Для CPU версии:
pip install torch torchvision
```

### GUI не открывается
- Убедитесь, что установлен Tkinter (обычно входит в Python)
- Установите Pillow: `pip install Pillow`

## 📚 Подробная документация

- **Новый GUI:** [signvoice_ai/README_GUI.md](signvoice_ai/README_GUI.md)
- **Полная документация:** [signvoice_ai/README.md](signvoice_ai/README.md)

## 💡 Советы

1. **Хорошее освещение** - убедитесь, что ваша рука хорошо освещена
2. **Контрастный фон** - используйте однотонный фон
3. **Стабильность** - держите жест стабильно несколько секунд
4. **Расстояние** - держите руку на расстоянии 30-60 см от камеры
5. **Настройки** - экспериментируйте с настройками для лучшего результата

## 🎉 Готово!

Теперь вы готовы использовать SignVoiceAI! Наслаждайтесь распознаванием жестов! 🖐️✨


